Read docs/design-roadmap.md in full. Compare to the live site at https://agenticaiccounting.com (and the inspirations named in the roadmap when relevant — primary: choppingblock.ai; secondary: piccalil.li). Then pick exactly one of three actions: (a) ship the next bounded step on the roadmap as a PR with npm run typecheck + npm test validation, branching from agent/stylist-<short-desc>, appending to the Completed steps log, and citing CSS-level mobile-clean evidence in the PR body; (b) open a plan-only PR editing only docs/design-roadmap.md if observation contradicts the plan or a step needs splitting/reordering; or (c) stop and report nothing-to-ship if the roadmap is clean and no high-confidence step remains. Phase 0 mobile work is gating — if any horizontal overflow, pagination wrap, or footer/nav regression is visible at iPhone widths, that is the next step regardless of phase. Do not touch pipeline code (src/collectors/**, src/scoring/**, src/db/**, src/index.ts) — the allowlist will reject the PR.
