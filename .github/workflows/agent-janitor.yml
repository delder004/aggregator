name: agent-janitor

on:
  schedule:
    - cron: '0 14 * * *'
  workflow_dispatch:
    inputs:
      goal:
        description: 'Goal override (leave blank to use the default janitor goal)'
        required: false
        type: string

jobs:
  run-agent:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    concurrency:
      group: agent-janitor
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - run: npm ci

      - name: Run janitor agent
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
          AGGREGATOR_AGENT_ID: ${{ secrets.AGGREGATOR_AGENT_ID }}
          AGGREGATOR_ENV_ID: ${{ secrets.AGGREGATOR_ENV_ID }}
          GITHUB_REPO_TOKEN: ${{ secrets.AGENT_GITHUB_PAT }}
          GITHUB_REPO_URL: https://github.com/${{ github.repository }}
          GOAL_INPUT: ${{ inputs.goal }}
        run: |
          GOAL="${GOAL_INPUT:-Audit the site for correctness bugs only — data-accuracy, content-quality, broken/corrupted/stale data, off-topic articles, mislabeled records. Inspect D1 via cf_api. If you find a concrete fixable bug, ship a targeted fix with tsc+vitest validation and open a PR via github_api. If you only find improvement opportunities (SEO, copy, UX, new content surfaces), do NOT ship them — note them in your final message and stop. Those belong to the contributor agent.}"
          npx tsx scripts/run-site-agent.mts "$GOAL"
